---
title: Def Colour
sidebar_position: 1
---
# Def Colour (`enum (formatting colour)`)
# Default: `GOLD`
The default colour to stylise text with.  
Can be set with the `defaultFormatting` gamerule on servers.  

:::danger Restart required
Toggling this option requires a restart to take (full) effect.
:::
