# Unlimited Command
# Usage: /unlimited
Toggles your itemstack's limitability.  
When enabled on an itemstack, it will never run out.

## Parameters
This command has no parameters.

## Example
`/unlimited`
