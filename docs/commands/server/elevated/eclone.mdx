# Eclone Command
# Usage: /eclone \[entities]
Clones either the entity you're looking at or the entities matching the given selector.

## Parameters
- **Entities**
  + Type: `entity selector`
  + Default: `@t (targeted entity)`
  + The entities to clone 

## Example
`/eclone @e[type=zombie]`
