# Heal Command
# Usage: /heal \[players]
Replenishes your health and saturation

## Parameters
- **Param**
  + Type: `player selector`
  + Default: `@s (command source)`
  + The players to heal and feed

## Example
`/heal @a[name=!"jeb_"]`
