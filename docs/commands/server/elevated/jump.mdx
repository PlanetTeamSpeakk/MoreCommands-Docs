# Jump Command
# Usage: /jump
Teleports to wherever you're looking, at most 160 blocks away.

## Parameters
This command has no parameters

## Example
`/jump`

:::tip
For the best effect, it is recommended to bind this command to a key with the [Bind Command](../../client/bind).
:::
