# Silent Command
# Usage: /silent <command\>
Runs any command silently.

## Parameters
- **Command**
  + Type: `command`
  + Default: none, this parameter is required
  + The command to run

## Example
`/silent give @s minecraft:apple`
