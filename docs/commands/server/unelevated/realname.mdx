# Realname Command
# Usage: /realname <query\>
Searches for players whose nickname matches the given query.  
Adding formattings to the query is not required.

## Parameters
- **Query**
  + Type: `string`
  + Default: none, this parameter is required
  + The query to search for

## Example
`/realname PTS`
