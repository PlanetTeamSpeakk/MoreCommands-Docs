# Spawn Command
# Usage: /spawn \[player]
Sends you or someone else to spawn.

## Parameters
- **Player**
  + Type: `player`
  + Default: `@s (command source)`
  + The player to send to spawn.
    Passing this requires the `morecommands.spawn.others` permission.

## Example
`/spawn PlanetTeamSpeak`
