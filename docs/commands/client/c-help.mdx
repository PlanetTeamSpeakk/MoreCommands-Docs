# C Help Command
# Usage: /chelp \[command]
Exactly the same as the help command, but lists all client commands.

## Parameters
- **Command**
  + Type: `command`
  + Default: none, if omitted, lists all commands
  + The command to get help on.

## Example
`/help macro`
