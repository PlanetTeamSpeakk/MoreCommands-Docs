# Search Command
# Usage: /search <query\>
Searches through your entire chat history to see what messages with the given query were sent.  
Clicking any result will scroll to them in chat.

## Parameters
- **Query**
  + Type: `string`
  + Default: none, this parameter is required
  + The query to look for

## Example
`/search my father`
