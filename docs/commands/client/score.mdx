# Score Command
# Usage: /score
Tells you your current score without having to die to see it.  
Not to be confused with the [C Score Command](./c-score).

## Parameters
This command has no parameters.

## Example
`/score`
