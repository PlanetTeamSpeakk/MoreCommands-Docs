# C Data Command
# Usage: /cdata entity|block|item \[path] \[scale]
Like the data command, but readonly, client-side and supports getting item data too.

## Parameters
- **Path**
  + Type: `nbt path`
  + Default: none, specifies the entire NBT tag
  + The path to get

## Example
`/cdata item PowerTool`
