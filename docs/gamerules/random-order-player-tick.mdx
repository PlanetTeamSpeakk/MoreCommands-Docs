# Random Order Player Tick Rule
# Type: `boolean`
Whether players should be ticked in a random order rather than starting at whomever joined first. 
Makes things like dropparties more fair.

**Default value**: `true`
