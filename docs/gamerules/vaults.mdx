# Vaults Rule
# Type: `int`
How many vaults each player has. Set to 0 to disable vaults.

**Default value**: `3`

:::tip Permission overwrite
The value of this gamerule can be set per player with the permission `morecommands.vault.max.<i>`
where `<i>` is any number between 1 and 100.
:::
